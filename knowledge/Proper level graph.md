>[!Definition]
is a [[Level graph |level graph]] with edges only occurring between adjacent levels. 

We can make a [[Level graph|level graph]] a [[Proper level graph|proper level graph]] by subdividing the edges spanning on multiple levels to edges occurring only between adjacent levels. We can do that by introducing vertices on the intersections of the edge spanning on multiple levels and the levels. 
